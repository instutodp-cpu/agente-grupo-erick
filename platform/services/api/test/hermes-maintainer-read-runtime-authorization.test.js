'use strict';
const test=require('node:test'),assert=require('node:assert/strict');
const {authorizeHermesMaintainerReadRuntime,validateHermesMaintainerReadRuntimeAuthorization}=require('../src/core/hermes-maintainer-read-runtime-authorization');
const admission={contract_version:'hermes_maintainer_read_execution_admission_v1',status:'MAINTAINER_READ_EXECUTION_ADMISSION_PREPARED_STAGING',admission_valid:true,receipt_fingerprint:'receipt-v1:abc',capability_fingerprint:'sha256:aaa',mission_id:'m1',operation:'repository_read',repository:'instutodp-cpu/agente-grupo-erick',base_ref:'main',executor_id:'hermes-maintainer-staging',provider:'GITHUB',trusted_adapter_id:'github_read_only_staging',read_only:true,staging_only:true,credential_resolution_permitted:false,network_execution_permitted:false,write_authorized:false,provider_called:false,execution_performed:false,production_allowed:false,blockers:[]};
const control={environment:'staging',kill_switch_active:false,read_runtime_enabled:true};
test('authorizes only staging repository read runtime while write and production remain blocked',()=>{const r=authorizeHermesMaintainerReadRuntime(admission,control);assert.equal(r.authorization_valid,true);assert.equal(r.network_authorized,true);assert.equal(r.credentials_authorized,true);assert.equal(r.execution_allowed,true);assert.equal(r.write_authorized,false);assert.equal(r.provider_called,false);assert.equal(r.execution_performed,false);assert.equal(r.production_allowed,false);assert.equal(validateHermesMaintainerReadRuntimeAuthorization(r).valid,true);});
test('kill switch blocks authorization',()=>assert.equal(authorizeHermesMaintainerReadRuntime(admission,{...control,kill_switch_active:true}).authorization_valid,false));
test('disabled runtime blocks authorization',()=>assert.equal(authorizeHermesMaintainerReadRuntime(admission,{...control,read_runtime_enabled:false}).authorization_valid,false));
test('non repository read is blocked',()=>assert.equal(authorizeHermesMaintainerReadRuntime({...admission,operation:'ci_read'},control).authorization_valid,false));
test('production control is blocked',()=>assert.equal(authorizeHermesMaintainerReadRuntime(admission,{...control,environment:'production'}).authorization_valid,false));

'use strict';
const test=require('node:test');const assert=require('node:assert/strict');
const {buildHermesMaintainerExecutionReceipt,validateHermesMaintainerExecutionReceipt,verifyHermesMaintainerExecutionReceipt}=require('../src/core/hermes-maintainer-execution-receipt-reconciliation');
function execution(){return{contract_version:'hermes_maintainer_controlled_executor_v1',mission_id:'m1',operation:'repository_read',repository:'instutodp-cpu/agente-grupo-erick',base_ref:'main',status:'MAINTAINER_CONTROLLED_EXECUTION_PREPARED_SIMULATION',execution_prepared:true,execution_eligible:false,execution_authorized:false,network_authorized:false,credentials_authorized:false,write_authorized:false,simulation:true,production_allowed:false,executed:false,runtime_mutated:false,network_used:false,provider_called:false,secret_accessed:false,operational_authority_consumed:false,blockers:[]};}
test('reconciles zero-effect controlled execution deterministically',()=>{const e=execution(),r=buildHermesMaintainerExecutionReceipt(e);assert.equal(r.reconciled,true);assert.equal(r.production_effect,'ZERO');assert.equal(validateHermesMaintainerExecutionReceipt(r).valid,true);assert.equal(verifyHermesMaintainerExecutionReceipt(e,r).valid,true);});
test('execution drift invalidates receipt',()=>{const e=execution(),r=buildHermesMaintainerExecutionReceipt(e);assert.equal(verifyHermesMaintainerExecutionReceipt({...e,base_ref:'other'},r).valid,false);});
test('claimed side effect blocks reconciliation',()=>{const r=buildHermesMaintainerExecutionReceipt({...execution(),runtime_mutated:true});assert.equal(r.reconciled,false);assert.equal(r.status,'MAINTAINER_EXECUTION_RECEIPT_RECONCILIATION_BLOCKED');});
test('validator rejects claimed production effect',()=>{const r=buildHermesMaintainerExecutionReceipt(execution());assert.equal(validateHermesMaintainerExecutionReceipt({...r,production_effect:'NON_ZERO'}).valid,false);});
